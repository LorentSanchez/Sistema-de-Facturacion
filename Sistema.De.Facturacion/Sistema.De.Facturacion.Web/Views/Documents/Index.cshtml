@model IEnumerable<Sistema.De.Facturacion.Web.Models.Document>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Documento)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Documento)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ID_Doc">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.ID_Doc">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.ID_Doc">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
